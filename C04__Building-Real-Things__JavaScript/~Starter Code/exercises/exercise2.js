const array=[true,false,false,true,true,false,false,false]
const count = array.filter(Boolean).length;

//Function needs to  Create a function that returns the number of true values in an array.
//needs a loop through the array to find all the true values
// needs to return that number
//look at the exercise where we seperated the array into multiple arrays maybe to create arrays that hold the true and false boolean values


function seperateArray(array) {

console.log(count);

}
seperateArray()